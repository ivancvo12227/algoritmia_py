print("este programa determina que numeros son positivos y cuales negativos y cuantos son ceros")

# Definir un vector de 20 posiciones
vector = [0] * 20

# Solicitar los valores al usuario
for i in range(20):
    num = int(input("Ingrese un numero " + str(i + 1) + ": "))
    vector[i] = num


positivos = 0
negativos = 0
ceros = 0

for num in vector:
    if num > 0:
        positivos += 1
    elif num < 0:
        negativos += 1
    else:
        ceros += 1

# resultados
print("Positivos:", positivos)
print("Negativos:", negativos)
print("Ceros:", ceros)
